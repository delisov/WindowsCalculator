<UserControl x:Class="CalcDmitriyElisov.Views.CalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CalcDmitriyElisov"
             xmlns:views="clr-namespace:CalcDmitriyElisov.Views"
             xmlns:commands="clr-namespace:CalcDmitriyElisov.Commands"
             d:DesignHeight="640" d:DesignWidth="400"
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">
    <DockPanel >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <RowDefinition Height="104*" />
                <RowDefinition Height="34" />
                <RowDefinition Height="355*"/>
            </Grid.RowDefinitions>
            <views:ScreenView x:Name="ScreenView" Width="Auto"  Grid.Row="1"></views:ScreenView>
            <views:MemView Grid.Row="2"></views:MemView>
            <views:PadView x:Name="PadView" Height="Auto" Width="Auto" Grid.Row="3"></views:PadView>

        </Grid>
    </DockPanel>
</UserControl>
